import CreateContact from "../components/form/create-contact";

const ContactForm = () => {
  return (
    <section>
      <h1>Add Contact</h1>
      <CreateContact />
    </section>
  );
};

export default ContactForm;
